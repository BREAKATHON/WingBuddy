doctype html
html(lang='en')
 head
   title Landing Page
 body
   h1 #{value}
   div.remark
     p #{error == undefined ? "No error" : error.message}   div.remark
     p #{error == undefined ? "No error" : error.message}   div.remark
     p #{error == undefined ? "No error" : error.message}